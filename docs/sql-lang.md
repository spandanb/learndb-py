This outlines the input language (~subset of ANSI sql) that is supported.

The grammar can be found in the <src>/lang_parser/grammar.py file

Some key callouts,

- all columns must be explicitly selected, i.e. no support for `select *`



